services:
  db:
    container_name: atlasbase-db
    image: postgres:latest
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=atlasbasedb
      - POSTGRES_USER=atlasebase_admin
      - POSTGRES_PASSWORD=atlasbase_admin


